import { environment } from "./environment";

export const constant = {
    login: `${environment.API_URL}/login`,
    register: `${environment.API_URL}/register`,
    user: `${environment.API_URL}/users`,
};
